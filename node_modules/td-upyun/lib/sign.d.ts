import { Service } from './service';
export declare type headerSignGetter = (service: Service, method: string, path: string, contentMd5?: string | null) => {
    Authorization: string;
    'X-Date': string;
};
/**
 * generate head sign
 * @param {object} service
 * @param {string} path - storage path on upyun server, e.g: /your/dir/example.txt
 * @param {string} contentMd5 - md5 of the file that will be uploaded
 */
export declare function getHeaderSign(service: Service, method: string, path: string, contentMd5?: string | null): {
    'Authorization': string;
    'X-Date': string;
};
/**
 * generate signature string which can be used in head sign or body sign
 * @param {object} service
 * @param {object} options - must include key is method, path
 */
export declare function genSign(service: Service, options: any): string;
/**
 * get policy and authorization for form api
 * @param {object} service
 * @param {object} - other optional params @see http://docs.upyun.com/api/form_api/#_2
 */
export declare function getPolicyAndAuthorization(service: Service, params: any): {
    policy: string;
    authorization: string;
};
export declare function getPurgeHeaderSign(service: Service, urls: string[]): {
    'Authorization': string;
    'Date': string;
    'User-Agent': string;
};
